package {{package}};

import jakarta.servlet.http.*;
import org.springframework.http.*;
import org.springframework.web.bind.annotation.*;
import java.util.*;
{{#each customAttributes.imports}}
    import {{this}};
{{/each}}

{{#if customAttributes.enableSpringSecurityAuthPrincipal}}
    import org.springframework.security.core.annotation.CurrentSecurityContext;
    import org.springframework.security.core.Authentication;
{{/if}}

{{> documentation }}
@jakarta.annotation.Generated(value = "{{generatorOpts.generatorClassName}}", date = "{{generatorOpts.generationDate}}")
public interface {{typeName}} {
{{#each methods}}
    {{> requestsHandlerMethod}}
{{/each}}
}