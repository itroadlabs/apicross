{{#if primitive}}
    {{#if defaultValueNotNull}}
        {{#if string}}
            = JsonNullable.of("{{{defaultValue}}}")
        {{else}}
            = JsonNullable.of({{{defaultValue}}}{{#if integer}}{{#eq format 'int64'}}L{{/eq}}{{/if}})
        {{/if}}
    {{else}}
        = JsonNullable.undefined()
    {{/if}}
{{else}}
    = JsonNullable.undefined()
{{/if}}